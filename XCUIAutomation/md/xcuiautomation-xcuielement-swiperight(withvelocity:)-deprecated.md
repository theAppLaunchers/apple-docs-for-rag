

- XCUIAutomation
- XCUIElement
-  swipeRight(withVelocity:) Deprecated

Instance Method

# swipeRight(withVelocity:)

Sends a swipe-right gesture with a velocity you specify.

Xcode 16.3+watchOS

``` source
@MainActor @nonobjc @preconcurrency
func swipeRight(withVelocity velocity: XCUIGestureVelocity)
```

Deprecated

Use swipeRight(velocity:) instead.

## Parameters 

`velocity`  

The speed at which to perform the swipe-right gesture, expressed in pixels per second.

## See Also

### Deprecated methods

func swipeDown(withVelocity: XCUIGestureVelocity)

Sends a swipe-down gesture with a velocity you specify.

Deprecated

func swipeUp(withVelocity: XCUIGestureVelocity)

Sends a swipe-up gesture with a velocity you specify.

Deprecated

func swipeLeft(withVelocity: XCUIGestureVelocity)

Sends a swipe-left gesture with a velocity you specify.

Deprecated

