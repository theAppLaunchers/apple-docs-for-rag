

- XCUIAutomation
- XCUIDeviceHandGesture
-  hash(into:) 

Instance Method

# hash(into:)

XCUIAutomationSwiftwatchOSXcode 16.3+

``` source
func hash(into hasher: inout Hasher)
```

Available when `Self` conforms to `Hashable` and `RawValue` conforms to `Hashable`.

