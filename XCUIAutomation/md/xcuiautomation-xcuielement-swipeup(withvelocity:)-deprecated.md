

- XCUIAutomation
- XCUIElement
-  swipeUp(withVelocity:) Deprecated

Instance Method

# swipeUp(withVelocity:)

Sends a swipe-up gesture with a velocity you specify.

Xcode 16.3+watchOS

``` source
@MainActor @nonobjc @preconcurrency
func swipeUp(withVelocity velocity: XCUIGestureVelocity)
```

Deprecated

Use swipeUp(velocity:) instead.

## Parameters 

`velocity`  

The speed at which to perform the swipe-up gesture, expressed in pixels per second.

## See Also

### Deprecated methods

func swipeDown(withVelocity: XCUIGestureVelocity)

Sends a swipe-down gesture with a velocity you specify.

Deprecated

func swipeLeft(withVelocity: XCUIGestureVelocity)

Sends a swipe-left gesture with a velocity you specify.

Deprecated

func swipeRight(withVelocity: XCUIGestureVelocity)

Sends a swipe-right gesture with a velocity you specify.

Deprecated

