

- XCUIAutomation
- XCUIDevice
- XCUIDevice.Button
-  hashValue 

Instance Property

# hashValue

XCUIAutomationSwiftiOSiPadOSMac CatalysttvOSvisionOSwatchOSXcode 16.3+

``` source
var hashValue: Int { get }
```

Available when `Self` conforms to `Hashable` and `RawValue` conforms to `Hashable`.

