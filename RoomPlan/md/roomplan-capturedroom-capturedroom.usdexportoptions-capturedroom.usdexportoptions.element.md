

- RoomPlan
- CapturedRoom
- CapturedRoom.USDExportOptions
-  CapturedRoom.USDExportOptions.Element 

Type Alias

# CapturedRoom.USDExportOptions.Element

The element type of the option set.

iOS 16.0+iPadOS 16.0+Mac Catalyst 16.0+

``` source
typealias Element = CapturedRoom.USDExportOptions
```

## Discussion

To inherit all the default implementations from the `OptionSet` protocol, the `Element` type must be `Self`, the default.

