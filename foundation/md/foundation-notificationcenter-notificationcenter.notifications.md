

- Foundation
- NotificationCenter
-  NotificationCenter.Notifications 

Class

# NotificationCenter.Notifications

An asynchronous sequence of notifications generated by a notification center.

iOS 15.0+iPadOS 15.0+Mac Catalyst 15.0+macOS 12.0+tvOS 15.0+visionOS 1.0+watchOS 8.0+

``` source
final class Notifications
```

## Overview

Use the notifications(named:object:) method to create an instance of this type, then iterate over its elements with the `for`-`await`-`in` syntax.

Tip

The Notification type doesn’t conform to Sendable, so iterating over this asynchronous sequence produces a compiler warning. You can use a `map(_:)-3wc5j` or `compactMap(_:)-6wlbd` operator on the sequence to extract sendable properties of the notification and iterate over those instead. See notifications(named:object:) for an example of this approach.

## Topics

### Creating an Iterator

struct Iterator

The asynchronous iterator created by this asynchronous sequence.

## Relationships

### Conforms To

- AsyncSequence
- Sendable

## See Also

### Receiving Notifications as an Asynchronous Sequence

func notifications(named: Notification.Name, object: (any AnyObject &amp; Sendable)?) -> NotificationCenter.Notifications

Returns an asynchronous sequence of notifications produced by this center for a given notification name and optional source object.

