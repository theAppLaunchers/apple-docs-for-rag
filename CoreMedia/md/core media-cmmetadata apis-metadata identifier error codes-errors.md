

- Core Media
- CMMetadata APIs
-  Metadata Identifier Error Codes 

API Collection

# Metadata Identifier Error Codes

Error codes that indicate metadata identifier errors.

## Topics

### Constants

var kCMMetadataIdentifierError_AllocationFailed: OSStatus

An error that indicates an allocation fails.

var kCMMetadataIdentifierError_BadIdentifier: OSStatus

An error that indicates the identifier isn’t valid.

var kCMMetadataIdentifierError_BadKey: OSStatus

An error that indicates a key isn’t valid.

var kCMMetadataIdentifierError_BadKeyLength: OSStatus

An error that indicates that a key doesn’t have the correct length.

var kCMMetadataIdentifierError_BadKeySpace: OSStatus

An error that indicates the keyspace isn’t valid.

var kCMMetadataIdentifierError_BadKeyType: OSStatus

An error that indicates a key has a bad type.

var kCMMetadataIdentifierError_BadNumberKey: OSStatus

An error that indicates the number for a key isn’t valid.

var kCMMetadataIdentifierError_NoKeyValueAvailable: OSStatus

An error that indicates a request for a key value in the anonymous keyspace.

var kCMMetadataIdentifierError_RequiredParameterMissing: OSStatus

An error that indicates a parameter the function requires is empty.

## See Also

### Constants

Metadata Registry Error Codes

Error codes that indicate metadata registry errors.

Metadata Identifier Keyspaces

Constants that describe metadata identifier keyspaces.

Metadata Identifiers

Constants that describe metadata identifiers.

Metadata Base Data Types

Constants that describe metadata base data types.

Metadata Data Types

Constants that describe metadata data types.

