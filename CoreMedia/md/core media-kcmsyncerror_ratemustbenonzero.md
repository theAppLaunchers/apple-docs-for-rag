

- Core Media
-  kCMSyncError_RateMustBeNonZero 

Global Variable

# kCMSyncError_RateMustBeNonZero

A sync error that indicates the rate is nonzero.

iOSiPadOSMac CatalystmacOStvOSvisionOSwatchOS

``` source
var kCMSyncError_RateMustBeNonZero: OSStatus { get }
```

## See Also

### Constants

var kCMSyncError_MissingRequiredParameter: OSStatus

A sync error that indicates a parameter is missing.

var kCMSyncError_InvalidParameter: OSStatus

A sync error that indicates a parameter isn’t valid.

var kCMSyncError_AllocationFailed: OSStatus

A sync error that indicates the memory allocation fails.

