

- MapKit JS
- mapkit.PlaceLookup
-  mapkit.PlaceLookup 

Initializer

# mapkit.PlaceLookup

Creates a place lookup with a set of options.

MapKit JS 5.78.1+

``` source
new mapkit.PlaceLookup(
 optional PlaceLookupOptions options
);
```

