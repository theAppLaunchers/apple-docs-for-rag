

- MapKit JS
- mapkit.Map
-  addTileOverlays 

Instance Method

# addTileOverlays

Adds an array of tile overlays to the map.

MapKit JS 5.0+

``` source
mapkit.TileOverlay[] addTileOverlays(
 mapkit.TileOverlay[] tileOverlays
);
```

## Parameters 

`tileOverlays`  

An array of tile overlays to add.

## Return Value

Returns the array of tile overlays.

## See Also

### Adding and removing tile overlays

tileOverlays

An array of all of the map’s tile overlays.

addTileOverlay

Adds a tile overlay to the map.

removeTileOverlay

Removes a tile overlay from the map.

removeTileOverlays

Removes an array of tile overlays from the map.

