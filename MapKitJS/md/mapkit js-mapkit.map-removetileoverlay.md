

- MapKit JS
- mapkit.Map
-  removeTileOverlay 

Instance Method

# removeTileOverlay

Removes a tile overlay from the map.

MapKit JS 5.0+

``` source
mapkit.TileOverlay removeTileOverlay(
 mapkit.TileOverlay tileOverlay
);
```

## Parameters 

`tileOverlay`  

The tile overlay to remove.

## Return Value

Returns the tile overlay.

## See Also

### Adding and removing tile overlays

tileOverlays

An array of all of the map’s tile overlays.

addTileOverlay

Adds a tile overlay to the map.

addTileOverlays

Adds an array of tile overlays to the map.

removeTileOverlays

Removes an array of tile overlays from the map.

