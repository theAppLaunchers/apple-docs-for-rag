

- ShaderGraph
- Math
-  Clamp 

ShaderGraph Node

# Clamp

Clamps the input per-channel to a specified range.

## Parameter Types

- Clamp (float)
- Clamp (long)
- Clamp (vector4f)
- Clamp (color3f)
- Clamp (vector2f)
- Clamp (vector3ulong)
- Clamp (color4f)
- Clamp (color3f FA)
- Clamp (vector3h)
- Clamp (vector2i)
- Clamp (integer)
- Clamp (vector4h FA)
- Clamp (ulong)
- Clamp (ushort)
- Clamp (vector3uinteger)
- Clamp (vector2f FA)
- Clamp (vector4h)
- Clamp (vector3i)
- Clamp (vector3h FA)
- Clamp (vector2h FA)
- Clamp (vector3f FA)
- Clamp (uinteger)
- Clamp (vector3long)
- Clamp (vector3ushort)
- Clamp (vector2ushort)
- Clamp (vector2ulong)
- Clamp (vector4f FA)
- Clamp (vector2h)
- Clamp (vector2uinteger)
- Clamp (short)
- Clamp (vector3f)
- Clamp (color4f FA)
- Clamp (vector3short)
- Clamp (vector2long)
- Clamp (half)
- Clamp (vector2short)

| Input  | Type  |
|--------|-------|
| `In`   | Float |
| `Low`  | Float |
| `High` | Float |

| Output | Type  |
|--------|-------|
| `Out`  | Float |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector4f |
| `Low`  | Vector4f |
| `High` | Vector4f |

| Output | Type     |
|--------|----------|
| `Out`  | Vector4f |

| Input  | Type   |
|--------|--------|
| `In`   | Color3 |
| `Low`  | Color3 |
| `High` | Color3 |

| Output | Type   |
|--------|--------|
| `Out`  | Color3 |

| Input  | Type     |
|--------|----------|
| `In`   | Vector2f |
| `Low`  | Vector2f |
| `High` | Vector2f |

| Output | Type     |
|--------|----------|
| `Out`  | Vector2f |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | Color4 |
| `Low`  | Color4 |
| `High` | Color4 |

| Output | Type   |
|--------|--------|
| `Out`  | Color4 |

| Input  | Type   |
|--------|--------|
| `In`   | Color3 |
| `Low`  | Float  |
| `High` | Float  |

| Output | Type   |
|--------|--------|
| `Out`  | Color3 |

| Input  | Type     |
|--------|----------|
| `In`   | Vector3h |
| `Low`  | Vector3h |
| `High` | Vector3h |

| Output | Type     |
|--------|----------|
| `Out`  | Vector3h |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector4h |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector4h |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector2f |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector2f |

| Input  | Type     |
|--------|----------|
| `In`   | Vector4h |
| `Low`  | Vector4h |
| `High` | Vector4h |

| Output | Type     |
|--------|----------|
| `Out`  | Vector4h |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector3h |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector3h |

| Input  | Type     |
|--------|----------|
| `In`   | Vector2h |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector2h |

| Input  | Type     |
|--------|----------|
| `In`   | Vector3f |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector3f |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector4f |
| `Low`  | Float    |
| `High` | Float    |

| Output | Type     |
|--------|----------|
| `Out`  | Vector4f |

| Input  | Type     |
|--------|----------|
| `In`   | Vector2h |
| `Low`  | Vector2h |
| `High` | Vector2h |

| Output | Type     |
|--------|----------|
| `Out`  | Vector2h |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type     |
|--------|----------|
| `In`   | Vector3f |
| `Low`  | Vector3f |
| `High` | Vector3f |

| Output | Type     |
|--------|----------|
| `Out`  | Vector3f |

| Input  | Type   |
|--------|--------|
| `In`   | Color4 |
| `Low`  | Float  |
| `High` | Float  |

| Output | Type   |
|--------|--------|
| `Out`  | Color4 |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

| Input  | Type |
|--------|------|
| `In`   | Half |
| `Low`  | Half |
| `High` | Half |

| Output | Type |
|--------|------|
| `Out`  | Half |

| Input  | Type   |
|--------|--------|
| `In`   | String |
| `Low`  | String |
| `High` | String |

| Output | Type   |
|--------|--------|
| `Out`  | String |

## Parameter descriptions

`In`  
The input to be clamped.

`Low`  
The low end of the clamp range.

`High`  
The high end of the clamp range.

## Discussion

The Clamp node restricts the range of values of an input; the range of values is defined by the `Low` and `High` parameters passed into the node. The output of the Clamp node is the same as the `In` value if it falls within the defined range. Otherwise, the output is clamped to the nearest limit, which is either the `Low` or `High` value. Use the Clamp node to create more predictable and controlled shader behavior for materials.

## See Also

### Nodes

Add

Adds two values.

Subtract

Subtracts two values.

Multiply

Multiplies two values.

Divide

Divides two values.

Modulo

Outputs the remaining fraction after dividing the input by a value and subtracting the integer portion.

Abs

Outputs the per-channel absolute value of the input.

Floor

Outputs the nearest integer value, per-channel, less than or equal to the incoming values.

Ceiling

Outputs the nearest integer value, per-channel, greater than or equal to the incoming values.

Power

Raises the incoming value to an exponent.

Sin

The sine of the incoming value in radians.

Cos

The cosine of the incoming value in radians.

Tan

The tangent of the incoming value in radians.

Asin

The arcsine of the incoming value in radians.

Acos

The arccosine of the incoming value in radians.

Atan2

The arctangent of the expression (iny/inx) in radians.

