

- Compositor Services
- CompositorLayer
-  init(configuration:renderer:) 

Initializer

# init(configuration:renderer:)

CompositorServicesSwiftUIvisionOS 2.0+

``` source
nonisolated
init(
    configuration: any CompositorLayerConfiguration = .default,
    renderer: @escaping @MainActor (LayerRenderer) -> Void
)
```

