

- Compositor Services
- LayerRenderer
- LayerRenderer.Frame
-  binocularFrustumMatrix(convention:increaseTangents:depthRange:) 

Instance Method

# binocularFrustumMatrix(convention:increaseTangents:depthRange:)

visionOS 2.0+

``` source
func binocularFrustumMatrix(
    convention: AxisDirectionConvention = .rightUpBack,
    increaseTangents: SIMD4,
    depthRange: SIMD2
) -> matrix_float4x4
```

