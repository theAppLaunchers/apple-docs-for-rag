

- AppKit
- NSSharingServicePicker
- NSSharingServicePicker.CollaborationModeRestriction
-  init(disabledMode:alertTitle:alertMessage:) 

Initializer

# init(disabledMode:alertTitle:alertMessage:)

macOS 15.0+

``` source
init(
    disabledMode: NSSharingCollaborationMode,
    alertTitle: String,
    alertMessage: String
)
```

## Parameters 

`disabledMode`  

The disabled type of sharing

`alertTitle`  

The alert title

`alertMessage`  

The alert message

