

- AppKit
- NSSharingServicePicker
- NSSharingServicePicker.CollaborationModeRestriction
-  init(disabledMode:alertTitle:alertMessage:alertDismissButtonTitle:) 

Initializer

# init(disabledMode:alertTitle:alertMessage:alertDismissButtonTitle:)

macOS 15.0+

``` source
init(
    disabledMode: NSSharingCollaborationMode,
    alertTitle: String,
    alertMessage: String,
    alertDismissButtonTitle: String
)
```

## Parameters 

`disabledMode`  

The disabled type of sharing

`alertTitle`  

The alert title

`alertMessage`  

The alert message

`alertDismissButtonTitle`  

The label on the default alert button

