

- Quick Look
- PreviewItem
-  init(url:displayName:editingMode:) 

Initializer

# init(url:displayName:editingMode:)

Create a preview item from a URL and, optionally, a displayName and editingMode.

visionOS 2.0+

``` source
init(
    url: URL,
    displayName: String? = nil,
    editingMode: EditingMode = .updateContents
)
```

