

- Core Animation
- CALayer
-  createRemoteLayerBound(to:) Deprecated

Instance Method

# createRemoteLayerBound(to:)

macOS 10.10–11.0Deprecated

``` source
func createRemoteLayerBound(to serverPort: mach_port_t) -> (any JRSRemoteLayer & NSObjectProtocol)!
```

Deprecated

This functionality is no longer supported as it does not seem to be used in modern versions of Java. It may be removed in a future version of macOS.

