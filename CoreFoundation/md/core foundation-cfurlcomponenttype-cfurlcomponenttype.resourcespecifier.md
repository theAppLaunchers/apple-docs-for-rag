

- Core Foundation
- CFURLComponentType
-  CFURLComponentType.resourceSpecifier 

Case

# CFURLComponentType.resourceSpecifier

The URL’s resource specifier.

iOSiPadOSMac CatalystmacOStvOSvisionOSwatchOS

``` source
case resourceSpecifier
```

## See Also

### Constants

case scheme

The URL’s scheme.

case netLocation

The URL’s network location.

case path

The URL’s path component.

case user

The URL’s user.

case password

The user’s password.

case userInfo

The user’s information.

case host

The URL’s host.

case port

The URL’s port.

case parameterString

The URL’s parameter string.

case query

The URL’s query.

case fragment

The URL’s fragment.

