

- Endpoint Security
- es_muted_process_t
-  init(audit_token:event_count:events:) 

Initializer

# init(audit_token:event_count:events:)

Mac CatalystmacOS

``` source
init(
    audit_token: audit_token_t,
    event_count: Int,
    events: UnsafePointer!
)
```

