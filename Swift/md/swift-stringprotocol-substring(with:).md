

- Swift
- StringProtocol
-  substring(with:) 

Instance Method

# substring(with:)

Returns a string object containing the characters of the `String` that lie within a given range.

iOS 8.0+iPadOS 8.0+Mac Catalyst 8.0+macOS 10.10+tvOS 9.0+visionOS 1.0+watchOS 2.0+SwiftDeprecated

``` source
func substring(with aRange: Range) -> String
```

