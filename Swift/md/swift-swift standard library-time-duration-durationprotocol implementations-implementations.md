

- Swift
- Swift Standard Library
- Time
- Duration
-  DurationProtocol Implementations 

API Collection

# DurationProtocol Implementations

## Topics

### Operators

static func * &lt;T>(Duration, T) -> Duration

static func *= (inout Self, Int)

static func / (Duration, Duration) -> Double

static func / &lt;T>(Duration, T) -> Duration

static func /= (inout Self, Int)

