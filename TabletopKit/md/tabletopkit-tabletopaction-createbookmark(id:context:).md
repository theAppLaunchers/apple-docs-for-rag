

- TabletopKit
- TabletopAction
-  createBookmark(id:context:) 

Type Method

# createBookmark(id:context:)

visionOS 2.0+

``` source
static func createBookmark(
    id: StateBookmarkIdentifier,
    context: UInt64 = 0
) -> Self
```

Available when `Self` is `CreateBookmarkAction`.

## See Also

### Creating bookmarks

static func createBookmark(StateBookmark, context: UInt64) -> Self

