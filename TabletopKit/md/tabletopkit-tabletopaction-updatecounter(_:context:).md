

- TabletopKit
- TabletopAction
-  updateCounter(\_:context:) 

Type Method

# updateCounter(\_:context:)

visionOS 2.0+

``` source
static func updateCounter(
    _ counter: ScoreCounter,
    context: UInt64 = 0
) -> Self
```

Available when `Self` is `UpdateCounterAction`.

## See Also

### Keeping score

static func updateCounter(matching: ScoreCounter.Identifier, value: Int64, context: UInt64) -> Self

