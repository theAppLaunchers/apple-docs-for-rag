

- Quartz
- IKImageBrowserView
-  delegate 

Instance Property

# delegate

Returns the delegate of the receiver.

macOS 10.4+

``` source
@IBOutlet @MainActor
unowned(unsafe) var delegate: AnyObject! { get set }
```

## Return Value

The delegate.

