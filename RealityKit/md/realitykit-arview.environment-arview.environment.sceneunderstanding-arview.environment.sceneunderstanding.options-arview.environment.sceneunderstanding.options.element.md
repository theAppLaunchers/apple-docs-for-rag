

- RealityKit
- ARView
- 
  - ARView
- ARView.Environment
- ARView.Environment.SceneUnderstanding
- ARView.Environment.SceneUnderstanding.Options
-  ARView.Environment.SceneUnderstanding.Options.Element 

Type Alias

# ARView.Environment.SceneUnderstanding.Options.Element

The element type of the option set.

iOS 13.4+iPadOS 13.4+Mac Catalyst 14.0+macOS 10.15+

``` source
typealias Element = ARView.Environment.SceneUnderstanding.Options
```

## Discussion

To inherit all the default implementations from the `OptionSet` protocol, the `Element` type must be `Self`, the default.

