

- RealityKit
- PhotogrammetrySession
- PhotogrammetrySession.Configuration
- PhotogrammetrySession.Configuration.MeshPrimitive
-  ==(\_:\_:) 

Operator

# ==(\_:\_:)

Returns a Boolean value indicating whether two values are equal.

Mac Catalyst 18.0+macOS 15.0+

``` source
static func == (a: PhotogrammetrySession.Configuration.MeshPrimitive, b: PhotogrammetrySession.Configuration.MeshPrimitive) -> Bool
```

## Parameters 

`lhs`  

A value to compare.

`rhs`  

Another value to compare.

## Discussion

Equality is the inverse of inequality. For any values `a` and `b`, `a == b` implies that `a != b` is `false`.

