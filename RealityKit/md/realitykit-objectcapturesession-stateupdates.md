

- RealityKit
- ObjectCaptureSession
-  stateUpdates 

Instance Property

# stateUpdates

The `Updates` `AsyncSequence` for the `state` property.

RealityKitSwiftUIiOS 17.0+iPadOS 17.0+

``` source
@MainActor
var stateUpdates: ObjectCaptureSession.Updates { get }
```

