

- RealityKit
- ObjectCaptureView
-  init(session:) 

Initializer

# init(session:)

Renders the current state of the provided session.

RealityKitSwiftUIiOS 17.0+iPadOS 17.0+

``` source
nonisolated
init(session: ObjectCaptureSession) where Overlay == EmptyView
```

