

- RealityKit
- AttachmentContentBuilder
-  buildBlock(\_:) 

Type Method

# buildBlock(\_:)

Provides support for lists of Attachments to be created in parallel.

RealityKitSwiftUIvisionOS 1.0+

``` source
static func buildBlock(_ content: repeat each Content) -> TupleAttachmentContent where repeat each Content : AttachmentContent
```

