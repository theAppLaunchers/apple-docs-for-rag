

- DeviceActivity
- DeviceActivityReport
- DeviceActivityReport.Context
-  hashValue 

Instance Property

# hashValue

DeviceActivitySwiftUIiOS 15.0+iPadOS 15.0+Mac Catalyst 15.0+

``` source
var hashValue: Int { get }
```

Available when `Self` conforms to `Hashable` and `RawValue` conforms to `Hashable`.

