

- DeviceActivity
- DeviceActivityData
- DeviceActivityData.CategoryActivity
-  webDomains 

Instance Property

# webDomains

The user’s web domain activity that contributed to this category’s totalActivityDuration.

DeviceActivitySwiftUIiOS 16.0+iPadOS 16.0+Mac Catalyst 16.0+

``` source
var webDomains: DeviceActivityResults { get }
```

