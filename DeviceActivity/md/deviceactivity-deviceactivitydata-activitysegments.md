

- DeviceActivity
- DeviceActivityData
-  activitySegments 

Instance Property

# activitySegments

The activity of the user divided into segments.

DeviceActivitySwiftUIiOS 16.0+iPadOS 16.0+Mac Catalyst 16.0+

``` source
var activitySegments: DeviceActivityResults { get }
```

## Discussion

The length of each activity segment is dependent on the segmentInterval that your app requests via a DeviceActivityFilter.

