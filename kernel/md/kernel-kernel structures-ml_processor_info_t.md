

- Kernel
- Kernel Structures
-  ml_processor_info_t 

Structure

# ml_processor_info_t

macOS 11.0+

``` source
typedef struct ml_processor_info {
    ...
} ml_processor_info_t;
```

## Topics

### Instance Properties

cluster_id

cluster_type

cpu_id

idle_tickle

idle_timer

idle_timer_refcon

l2_access_penalty

l2_cache_id

l2_cache_size

l3_cache_id

l3_cache_size

log_id

phys_id

platform_cache_dispatch

platform_error_handler

powergate_latency

powergate_stub_addr

powergate_stub_length

processor_idle

regmap_paddr

start_paddr

supports_nap

time_base_enable

