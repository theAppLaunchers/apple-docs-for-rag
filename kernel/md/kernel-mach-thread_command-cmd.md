

- Kernel
- mach
- thread_command
-  cmd 

Instance Property

# cmd

Common to all load command structures. For this structure, set to `LC_THREAD` or `LC_UNIXTHREAD`.

macOS 10.6+

``` source
uint32_t cmd;
```

