

- SwiftUI
- SpatialTapGesture
-  init(count:coordinateSpace:) Deprecated

Initializer

# init(count:coordinateSpace:)

Creates a tap gesture with the number of required taps and the coordinate space of the gesture’s location.

iOS 16.0–18.4DeprecatediPadOS 16.0–18.4DeprecatedMac Catalyst 16.0–18.4DeprecatedmacOS 13.0–15.4DeprecatedvisionOS 1.0–2.4DeprecatedwatchOS 9.0–11.4Deprecated

``` source
init(
    count: Int = 1,
    coordinateSpace: CoordinateSpace = .local
)
```

Deprecated

Use init(count:coordinateSpace:) instead.

Show all declarations

## Parameters 

`count`  

The required number of taps to complete the tap gesture.

`coordinateSpace`  

The coordinate space of the tap gesture’s location.

