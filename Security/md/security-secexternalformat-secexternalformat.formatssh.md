

- Security
- SecExternalFormat
-  SecExternalFormat.formatSSH 

Case

# SecExternalFormat.formatSSH

OpenSSH 1 format for asymmetric (public/private) keys. OpenSSH is an OpenBSD implementation of the Secure Shell (SSH) protocol.

macOS 10.0+

``` source
case formatSSH
```

