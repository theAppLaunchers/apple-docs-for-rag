

- Security
- SecExternalFormat
-  SecExternalFormat.formatWrappedSSH 

Case

# SecExternalFormat.formatWrappedSSH

OpenSSH 1 format for wrapped symmetric and private keys. OpenSSH is an OpenBSD implementation of the Secure Shell (SSH) protocol.

macOS 10.0+

``` source
case formatWrappedSSH
```

