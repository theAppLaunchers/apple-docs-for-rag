

- Security
- SecExternalFormat
-  SecExternalFormat.formatRawKey 

Case

# SecExternalFormat.formatRawKey

Format for symmetric keys. Raw, unformatted key bits. This is the default for symmetric keys.

macOS 10.0+

``` source
case formatRawKey
```

