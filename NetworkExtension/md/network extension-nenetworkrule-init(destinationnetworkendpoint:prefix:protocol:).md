

- Network Extension
- NENetworkRule
-  init(destinationNetworkEndpoint:prefix:protocol:) 

Initializer

# init(destinationNetworkEndpoint:prefix:protocol:)

macOS 15.0+

``` source
convenience init(
    destinationNetworkEndpoint networkEndpoint: NWEndpoint,
    prefix destinationPrefix: Int,
    protocol: NENetworkRule.Protocol
)
```

