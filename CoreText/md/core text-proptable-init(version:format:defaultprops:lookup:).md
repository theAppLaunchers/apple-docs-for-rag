

- Core Text
- PropTable
-  init(version:format:defaultProps:lookup:) 

Initializer

# init(version:format:defaultProps:lookup:)

iOSiPadOSMac CatalystmacOStvOSvisionOSwatchOS

``` source
init(
    version: Fixed,
    format: UInt16,
    defaultProps: PropCharProperties,
    lookup: SFNTLookupTable
)
```

