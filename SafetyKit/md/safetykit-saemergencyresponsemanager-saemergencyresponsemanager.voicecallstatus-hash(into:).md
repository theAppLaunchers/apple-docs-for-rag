

- SafetyKit
- SAEmergencyResponseManager
- SAEmergencyResponseManager.VoiceCallStatus
-  hash(into:) 

Instance Method

# hash(into:)

SafetyKitSwiftiOS 16.0+iPadOS 16.0+macOS 13.0+watchOS 10.1+

``` source
func hash(into hasher: inout Hasher)
```

Available when `Self` conforms to `Hashable` and `RawValue` conforms to `Hashable`.

