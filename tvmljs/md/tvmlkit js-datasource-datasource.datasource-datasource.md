

- TVMLKit JS
- DataSource
- DataSource.DataSource
-  DataSource 

Instance Method

# DataSource

Creates a new data source object.

tvOS 13.0+

``` source
new DataSource(
    in Array items, 
    in Array indexTitle, 
    in int segmentSize
);
```

