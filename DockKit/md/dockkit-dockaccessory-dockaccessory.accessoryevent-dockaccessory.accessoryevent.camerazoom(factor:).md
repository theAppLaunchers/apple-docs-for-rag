

- DockKit
- DockAccessory
- DockAccessory.AccessoryEvent
-  DockAccessory.AccessoryEvent.cameraZoom(factor:) 

Case

# DockAccessory.AccessoryEvent.cameraZoom(factor:)

A camera zoom event.

iOS 17.4+iPadOS 17.4+Mac Catalyst 17.4+macOS 14.4+

``` source
case cameraZoom(factor: Double)
```

## Parameters 

`factor`  

A zoom multiplier. For example, a value of `2.0` doubles the size of an image’s subject and halves the field of view.

