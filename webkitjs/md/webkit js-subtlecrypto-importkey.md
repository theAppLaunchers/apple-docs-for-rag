

- WebKit JS
- SubtleCrypto
-  importKey 

Instance Method

# importKey

Safari Desktop 9.0+Safari Mobile 9.0+

``` source
Promise  importKey(
    KeyFormat format, 
    (BufferSource or JsonWebKey) keyData, 
    AlgorithmIdentifier algorithm, 
    boolen extractable, 
    sequence  keyUsages
);
```

