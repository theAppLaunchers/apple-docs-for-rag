

- WebKit JS
- WebGL2RenderingContext
-  bindBufferRange 

Instance Method

# bindBufferRange

Safari Desktop 10.0+Safari Mobile 10.0+

``` source
void bindBufferRange(
    GLenum target, 
    GLuint index, 
    WebGLBuffer? buffer, 
    GLintptr offset, 
    GLsizeiptr size
);
```

