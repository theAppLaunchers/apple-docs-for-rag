

- CKTool JS
- ArrayError
-  indices 

Instance Property

# indices

An array of indexes of elements that failed validation in the array.

CKTool JS 1.2.15+

``` source
attribute Array indices;
```

