

- Finder Sync
- FIFinderSyncProtocol
-  makeListenerEndpoint(forServiceName:itemURL:) 

Instance Method

# makeListenerEndpoint(forServiceName:itemURL:)

macOS 10.10+

``` source
optional func makeListenerEndpoint(
    forServiceName serviceName: NSFileProviderServiceName,
    itemURL: URL
) throws -> NSXPCListenerEndpoint
```

